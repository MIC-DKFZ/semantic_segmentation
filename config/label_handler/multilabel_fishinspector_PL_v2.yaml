_target_: src.data_handler.label_handler_fishinspector.MultiLabelSegmentationHandlerFI_PL_v2
num_classes: ${dataset.num_classes}
dtype: ${dataset.dtype_mask}
prefix: ${dataset.label_prefix}
postfix: ${dataset.label_postfix}