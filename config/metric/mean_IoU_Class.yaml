#@package _global_
METRIC:
  NAME: mIoU
  DURING_TRAIN: False
  METRICS:
    mIoU:
      _target_: utils.metric.IoU_Class
      num_classes: ${DATASET.NUM_CLASSES}
      labels: ${DATASET.CLASS_LABELS}